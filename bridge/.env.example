# Worker URL — where the Cloudflare Worker runs
WORKER_URL=http://localhost:8787

# Bridge HTTP API port
BRIDGE_PORT=3080

# Data directory
DATA_DIR=./bridge/data

# Max conversation history turns per user
MAX_HISTORY_TURNS=20
