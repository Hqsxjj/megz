{
  "name": "claude-wechat-bridge",
  "version": "1.0.0",
  "description": "Direct Claude → WeChat bridge via iLink ClawBot protocol, no OpenClaw dependency",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "login": "node index.js --login-only"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.88.0",
    "dotenv": "^16.4.0",
    "qrcode-terminal": "^0.12.0"
  },
  "engines": {
    "node": ">=22"
  }
}
